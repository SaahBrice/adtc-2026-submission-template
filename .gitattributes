# Normalize line endings to LF in the repo so shell scripts run on the Ubuntu
# evaluator regardless of the OS they were authored on.
* text=auto eol=lf
*.sh text eol=lf

# Treat model/image binaries as binary (never line-ending-converted).
*.gguf binary
*.png binary
*.jpg binary
*.pdf binary
