# Model weights — never commit these to git
model/*.gguf
model/*.bin
model/*.safetensors
model/*.partial
*.partial

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/

# Local profiler output
submission.json
audit.json

# App working directories (generated at runtime)
app/data/
app/output/
app/.venv/

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Native llama.cpp binaries (fetched by download_model.sh)
bin/
